// styles/theme.js

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#C31815", // Set primary color to 'jungle_green'
    },
    secondary: {
      main: "#fff", // Set primary color to 'jungle_green'
    },
  },
});

export default theme;
